public static boolean onOneLine(final Coordinate start, final Coordinate end)
{
    boolean onLine = new boolean();

    if(start.column == end.column || start.row == end.row)
    {
        return boolean = true;
    }
    else
    {
        return boolean = false;
    }
}
